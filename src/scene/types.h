#include "game/scene.h"

namespace scene {

    struct TitleScene {
        game::ScenePipeline pipeline;
    };

    struct GameScene {
        game::ScenePipeline pipeline;
    };

    struct RestartScene {
        game::ScenePipeline pipeline;
    };
}
